# SPEC: uPC1237 + VOM1271 + 2x MOSFET (встречно, общие истоки) — защита АС
Рабочая папка: /tmp/ssr-prot/work/uPC1237_SSR  (в путях промпта "/work/uPC1237_SSR" = эта папка; /work недоступен)

## Проверенные факты (использовать как есть, ссылаться на них)
uPC1237 (NEC, doc IC-1806A; полеченный OCR — nec2_ocr.txt):
 1: детекция перегрузки, порог 0.58/0.67/0.76 В (или 110 мкА при токовом управлении); схема детекции ВНЕШНЯЯ
 2: DC-offset: +0.62 В typ (0.54..0.70), -0.17 В typ (-0.12..-0.23), Ic2=12.5 мкА
 3: LATCH = конденсатор на GND; AUTO-RESET = прямо на GND
 4: AC-OFF детекция, порог 0.74 В typ (0.60..0.90), вход через R4+C4 (R4≈24 кОм при 40 В rms)
 5: GND   6: выход драйвера реле — открытый коллектор NPN на GND, Imax=80 мА, НАСЫЩЕН В НОРМЕ (LED горит = ключ открыт)
 7: RC задержки power-on: порог 2.06 В, V8=3.4 В -> T_ON = 0.931*R7*C7
 8: Vcc+R8 (R8=15 кОм при 45 В), Vcc=25..60 В, abs max 60 В, Pd=320 мВт
Формулы порога DC (Ra/Rb/Rc): +Vth=(2+Ra/Rc)*0.62 ; -Vth=-(0.17*(2+Ra/Rc)+12.5мкА*Ra[кОм]) ; при Ra=Rb=56к: +1.24 В, -1.04 В

VOM1271 (Vishay 83469 rev1.9): VOC 7.8 min / 8.4 typ @IF=10 мА (8.1@5, 8.7@20, 8.9@30); ISC 6/15/30/47 мкА при 5/10/20/30 мА;
 VF 1.2/1.4/1.6 В @10 мА; IFmax=50 мА; VRmax=5 В; ton=53 мкс, toff=65 мкс (CL=200 пФ, IF=20 мА); изоляция 3750 VRMS; SOP-4.
 Оценка для модели: R_off(разряд затвора) ≈ 65мкс/(2.3*200пФ) ≈ 140 кОм; t_off ≈ 2.3*140к*Ciss = 3.2e5*Ciss.
   Ciss 2нФ -> 0.64 мс; 5нФ -> 1.6 мс; 11.1нФ -> 3.5 мс.

MOSFET-кандидаты (даташиты локально): SUM50020EL (60В, 2.1мОм@10В, 2.6мОм@4.5В, Qg=126нК, Ciss=11.1нФ, TO-263);
 IAUC120N06S5N015 (60В, 1.6мОм typ/1.9мОм max @Vgs=7В, Qg=73.7нК); SQJ158EP — 33 мОм, НЕ подходит.
Критерий: Rds(on)<=10 мОм на прибор при Vgs=7.8 В и 100 °C; минимизировать Ciss*Qg (t_off).

## Жёсткие требования
R1 2xN-MOSFET Vds>=50 В (60 В), Rds(on)<=10 мОм/прибор при Vgs 7.8..8.4 В (25 °C) + таблица при 100 °C
R2 общие истоки (CS), стоки = AMP_OUT и SPK, затворы объединены (GATE), Rgs на CS, один VOM1271
R3 fail-safe: LED горит (вывод 6 насыщен) = ключ открыт; авария/сеть/обрыв = LED гаснет = ключ закрыт; инверсия запрещена
R4 LED-ветвь: +Vaux -> [стабилитрон послед.] -> R -> LED -> PIN6; IF=10..15 мА; R=(Vaux-VF-Vz)/IF;
   ЗАПРЕЩЁН кламп/стабилитрон "PIN6->GND"; мощность элементов <=0.5 Вт; иначе +12 В через NPN-буфер (база 4.7к от PIN6, эмиттер GND, коллектор = катод LED)
R5 U(PIN6) в закрытом состоянии <=45 В (конструктивно, не клампом)
R6 T_ON≈4 с: T_ON=0.931*R7*C7, номиналы E24
R7 T_DC<=1 с (RC на выводе 2), порог ±2 В через Ra=Rb=56к; дать ±Vth по формулам
R8 вывод 3 = latch (0.022 мкФ), обосновать
R9 R4/C4 для AC-OFF, отключение <=100 мс
R10 BOM: номиналы, допуски, мощности, типы
R11 измерить t_off; если >2 мс при 25 А/50 В -> активный разряд затвора (без нарушения R3) ИЛИ Ciss<=3..5 нФ
R12 E=∫V*I dt при выключении под током, сверка с SOA при 50 В

## Топология модели (имена цепей менять нельзя)
AMP_OUT — выход УМ; SPK — нагрузка; CS — общие истоки; GATE — затворы; VIOP/VION — выход VOM1271;
VCC45/VCC12 — питания; LED_A/LED_K; PIN6; VIN_DC(2), VIN_OVL(1), VAC(4), VDLY(7), VLATCH(3), VCCIC(8).

## Инструменты
LTspice НЕ установлен. Верификация: `nix shell nixpkgs#ngspice -c ngspice -b file.cir` (ngspice-45).
Ограничения ngspice-45: нет .step; .meas TRIG только `TRIG v(x) VAL=y`; FIND — простой вектор; PARAM ссылается на именованные скаляры;
 VDMOS `Ron=` игнорируется (работают Rd=/Rs=); в batch обязателен .print/.plot/.meas.

## Тест-кейсы (tests/TCn_*.cir, каждый прогоняется ngspice -b)
TC1 power-on: до T_ON ключ закрыт (|I(SPK)|<1 мА), после — IF=10..15 мА, V(GATE,CS)>=7 В
TC2 Rds(on): Id=10 A, 25 °C и 100 °C, суммарно <=20 мОм (измерять V(AMP_OUT,SPK)/Id)
TC3 DC +2 В: детект <= T_DC, LED гаснет, измерить t_off
TC4 DC -2 В (двуполярность)
TC5 перегрузка (вывод 1)
TC6 AC-OFF: спад AC -> отключение <=100 мс
TC7a latch / TC7b auto-reset
TC8 сигнал 1 кГц, 8/4 Ом, 20 Впик — искажения/ступенька
TC9 выключение под током 20 А / 50 В: E и вердикт по SOA
TC10 fail-safe: обрыв LED -> ключ закрыт и остаётся закрытым
TC11 худший случай: Vgs=7.8 В, 100 °C, <=20 мОм суммарно
TC12 подача питания при аварии на входе — ключ не должен открыться ни на мгновение

## Артефакты
uPC1237_SSR.cir, uPC1237_SSR.asc (LTspice IV, Version 4, только стандартные символы; для MOSFET символ nmos + .model VDMOS),
tests/TC*.cir, run_tests.sh (+ tests/results.json), BOM.csv, README.md, REPORT.md, REVIEW.md (аудит).
