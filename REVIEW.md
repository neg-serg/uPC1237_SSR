# REVIEW.md — независимый аудит проекта uPC1237_SSR

**Аудитор:** независимый агент (только чтение и запуск; файлы проекта не правились).
**Метод:** `nix shell nixpkgs#ngspice -c ngspice -b <file>` (ngspice-45) + повторные прогоны `run_tests.sh` с выводом в `/tmp` (`RESULTS`/`LOG_DIR` переопределены, проект не затронут).
**Дата:** 2026-09-23.

## 0. Снимок и оговорка о «движущейся цели»

Во время аудита проект активно переписывался параллельным агентом (mtime 17:33 → 17:48 MSK). Все выводы ниже привязаны к **последнему снимку**, в котором `REPORT_TESTS.md` уже существует:

| файл | mtime | sha256 |
|---|---|---|
| `uPC1237_SSR.cir` | 17:46:53 | `ee1b3da3…56d39f5` |
| `tests/criteria.tsv` | 17:10:52 | `a8164480…a61a78a` |
| `tests/results.json` | 17:47:54 | `3f4b7daf…5c92110` |
| `REPORT_TESTS.md` | 17:48:39 | `5d8f1515…dd4d2cf` |
| `uPC1237_SSR.asc` | 17:35:14 | `e0c6489f…b9e91fb` |

Промежуточные состояния (17:39, 17:44) устарели и в выводах не используются. Важно: **мастер, тесты и `.asc` — из разных ревизий** (см. P1-1), а `tests/results.json` в момент 17:43 был внутренне противоречив (см. P1-4).

**Воспроизведение итога:**
```
cd /tmp/ssr-prot/work/uPC1237_SSR
ROOT=$PWD RESULTS=/tmp/audit/results_v4.json LOG_DIR=/tmp/audit/ci_v4 \
  bash run_tests.sh --no-wait      # exit 0, 12/12 PASS
nix shell nixpkgs#ngspice -c ngspice -b tests/TC3_dc_pos.cir   # t_off = 8.04123e-05
```
Мой независимый прогон совпал с `tests/results.json` и с `REPORT_TESTS.md` (12/12 PASS). До появления активного разряда затвора тот же набор давал 10/12 (FAIL: TC3, TC9) — см. P1-2/P2-1.

---

## 1. Вердикты по пяти утверждениям

### 1.1 «Rds(on) ≤ 10 мОм на прибор» — **ПОДТВЕРЖДЕНО** (с оговоркой по 100 °C)

Доказательства (все — `.meas`, из моего прогона, совпадают с `tests/results.json`):

| тест | `.meas` | значение | на прибор |
|---|---|---|---|
| `TC2_rds25` | `rds_key` | `-5.19865e-3` Ом | 2.60 мОм |
| `TC2b_rds100` | `rds_key` | `-5.89333e-3` Ом | 2.95 мОм |
| `TC11_worstcase` | `rds_each` | `3.09663e-3` Ом | 3.10 мОм (100 °C, VOC min) |

Критерий `criteria.tsv:30,31,67` — `abs<= 0.020` Ом на два прибора. Независимая проверка карты
`M_SSR` одним прибором (скретч-нетлист с `.meas`, вне проекта): `rds = 2.70200e-3` Ом @27 °C и
`rds = 3.08895e-3` Ом @100 °C. Паспорт: IAUC120N06S5N015 `RDS(on) max = 1.90 мОм` @Vgs=7 В, 25 °C
(`iauc.txt:154`), ×1.7 на 100 °C ⇒ ≤3.23 мОм. Запас к 10 мОм — более 3×.

Оговорки:
- `TC2b` — это **не** валидация 100 °C: ngspice VDMOS даёт лишь ×1.143 (2.702→3.089 мОм), тогда как
  даташитный коэффициент ×1.3…1.7. Инженерную оценку надо брать по даташиту (3.23 мОм), а не по `TC2b`.
  `README.md:80-81` это признаёт, `REPORT_TESTS.md` — нет.
- Знак `rds_key` отрицательный; критерий спасает `abs<=`. Единицы — Ом (0.020), не мОм.
- Расхождение партномеров: `SPEC.md:21` — `IAUC120N06S5L016`, а мастер/модель/BOM — `…N015` (см. P2-8).

### 1.2 «t_off измерен и обоснован» — **ПОДТВЕРЖДЕНО** (после добавления активного разряда)

Доказательства (`.meas`):
- `TC3_dc_pos.t_off = 8.04123e-05` с (80.4 мкс) ≤ 2 мс (`criteria.tsv:35`);
- `TC9_soa.t_off = 7.92397e-05` с (79.2 мкс) ≤ 2 мс (`criteria.tsv:60`);
- `TC4_dc_neg.t_off = 7.55369e-05` с; `TC6_acoff.t_off = 4.25323e-03` с (критерий ≤0.1 с).

Механизм проверен независимо: в мастере добавлены `BDIS`/`SDIS1`/`SDIS2` (`uPC1237_SSR.cir:65-68`,
`SWDIS Ron=50`), включаемые при `v(VLATCH)>4`. Скретч-проба `TC3` с дополнительными `.meas`:
`t_det=5.17621`, `t_g8=t_g5=t_gto(2.8 В)=t_g1=t_g01(0.1 В)=5.17629` — затвор падает с 8.36 до 0.1 В
за ~80 мкс (время набора защёлки). R11 прямо разрешает активный разряд, поэтому это законное решение.

Оговорки:
- Изменилось **определение** `t_off`: раньше «спад до 1 В», теперь «спад до `Vto=2.8 В`»
  (`REPORT_TESTS.md:29,44`). Определение защитимое, но в `criteria.tsv` оно не зафиксировано.
- Активный разряд включается **только по защёлке**: аварии без защёлки (AC-OFF, `TC6`) закрывают ключ
  пассивным путём 140 кОм (`t_off=4.25 мс`). Для R9 (≤100 мс) этого достаточно, но «t_off≤2 мс для любой
  аварии» — неверно.
- До фикса (мастер 17:44) `t_off` был 2.15…3.28 мс и **не проходил** R11 — именно поэтому разряд и добавлен.
- Разряд есть только в модели: в `BOM.csv` `OPT1 active gate discharge` до сих пор `TBD`.

### 1.3 «fail-safe: любая авария → ключ закрыт» — **ПОДТВЕРЖДЕНО** (на уровне поведенческой модели)

Доказательства (`.meas`, все PASS):
- DC ±: `TC3.vlatch_end=4.999995`, `TC4.vlatch_end=4.999995`; ключ закрыт (`t_vgs_off`).
- Перегрузка: `TC5.vovl=0.732785` (≥0.67), `TC5.vgs_off=1.85512e-06` (≤0.5), `TC5.vlatch=3.57487`.
- AC-OFF: `TC6.vac4_before=6.31382`, `t_off=4.25323e-3` (≤0.1 с).
- Обрыв LED: `TC10.vgs_before=8.35854` → `TC10.vgs_after=1.46714e-06` (≤0.5).
- Логика не инвертируется: `BLOGIC` (`uPC1237_SSR.cir:41`) = задержка · нет аварии · нет защёлки · сеть есть;
  LED питается только при `LOGIC` высоком. Активный разряд может только *закрыть* ключ, R3 не нарушает.

Оговорки: это макромодель uPC1237, а не кристалл; «любая» сильнее проверенного — нет тестов на потерю
вспомогательного +12 В/VCC, на обрыв `RGS` и на короткое замыкание самого MOSFET (отказ ключа fail-safe
в принципе не парируется).

### 1.4 «Ключ не открывается при подаче питания с аварией» — **ПОДТВЕРЖДЕНО**

Доказательства:
- `TC12_hot_start.vgs_max = 0.0143355` В (≤0.5), `vlatch = 4.999995` В (≥2.5) — авария +3 В с t=0.
- Независимо: тот же тест с `VAMP=2 В` (ровно на пороге R7) — `vgs_max = 0.0146482`, `vlatch=4.999995`,
  завершается с exit 0. То есть утверждение верно и для порогового случая.

Оговорка: генератор (`gen_tests.py` комментарий к `TC12`) перешёл с 2 В на 3 В, ссылаясь на «дребезг» и
потерю сходимости на пороге; на финальном мастере 2 В считается устойчиво, так что тест можно ужесточить.

### 1.5 «Порядок `.asc`/`.cir` совпадёт» — **ОПРОВЕРГНУТО**

Доказательства:
- `.asc` сгенерирован 17:35:14, мастер изменён 17:46:53 (и до этого в 17:39, 17:44) — `.asc` отстал на
  несколько ревизий.
- Сравнение элементов: в `.asc` **нет** `BCHG`, `BDIS`, `SDIS1`, `SDIS2`; лишний (удалённый из `.cir`)
  `RCHG` (`uPC1237_SSR.asc:123`). Значения: `RGS1/RGS2 = 1Meg` (`:233,240`), в `.cir` — `10Meg`.
- Регенерация `gen_schematic.py` из текущего `.cir` в изолированной копии (`/tmp/audit/schem`) даёт
  12+ строк расхождения; структурная проверка самого генератора проходит (41 элемент = 41 символ,
  все узлы `.cir` имеют `FLAG`). То есть генератор исправен — устарел закоммиченный `.asc`.

---

## 2. Замечания

### P1 — блокирующие для приёмки

1. **`.asc` не соответствует `.cir`** (см. §1.5): нет `BCHG`/`BDIS`/`SDIS1`/`SDIS2`, лишний `RCHG`,
   `RGS=1 МОм` вместо 10 МОм. Требуется перегенерировать `.asc` и добавить в CI проверку «`.asc` свежее
   `.cir`» (например, перегенерация в temp и `diff`).
2. **`REPORT.md` и `README.md` устарели и противоречат машинным результатам.** `REPORT.md:15` даёт
   `t_off=1.70 мс` (TC3, «PASS»), `REPORT.md:20,38,39` — `7.71 мс`/`E=0.174 Дж` (TC9), тогда как финал:
   `8.04123e-05` с и `e_off=4.11931e-3` Дж (в 42 раза меньше). Активный разряд (`BDIS`/`SDIS`) в этих
   отчётах не описан вовсе (упомянут лишь как *возможная* мера). Нарративные отчёты надо привести к
   финальному снимку.
3. **`BOM.csv` не выполняет R10.** 9× `TBD` (C4, R7, C7, Rc, Rgs, R_LED, D1, C_VCC, OPT1), при этом
   `Ra=Rb=56k` в BOM против `110k` в мастере; `OPT1 active gate discharge` помечен `TBD`, хотя в `.cir`
   уже реализован. BOM нужно собрать из финального мастера.
4. **CI считался на «плывущем» дереве.** `tests/results.json` (17:43) противоречил собственным `ci_logs`:
   `TC11.vgs_x=7.123198` против `6.99000` в `ci_logs/TC11_worstcase.log`; `TC5` — `exit 124` (timeout) при
   фактическом времени 0.7 с и PASS. Причина: мастер/тесты переписывались (17:39, 17:44, 17:46) во время
   выполнения `run_tests.sh`. Нужно фиксировать хэши входов в `results.json` и прерывать прогон при
   изменении входов, либо гонять CI на замороженной ревизии.

### P2 — существенные

1. **Смена определения `t_off`.** Критерий `criteria.tsv:35,60` говорит «key turn-off ≤2 мс», а тест меряет
   до `Vto=2.8 В`; пассивный baseline до 1 В (2.15…3.28 мс) остаётся худшим случаем и нигде не сохранён.
   Зафиксировать определение в критериях и в отчёте.
2. **Активный разряд только по защёлке** (`VLATCH>4`). Для не-защёлкивающихся аварий (AC-OFF) `t_off=4.25 мс`
   (TC6). Это допустимо для R9, но формулировку «t_off≤2 мс» нельзя распространять на все аварии.
3. **Обход `BOVL «/50»` в TC2/TC11 — законный, но недокументированный.** Проверено: с исходным `/5`
   детектор перегрузки закрывает ключ и `rds_key = -6.99712e+08` Ом (мусор: 80 В, 1.1e-7 А); с `/50` тест
   даёт корректные 5.2 мОм. Независимое измерение карты (2.702 мОм) совпадает с TC2, т.е. обход **не
   подделывает** Rds. Но `REPORT_TESTS.md` про `/50|BOVL|обход` не упоминает, а §4 утверждает, что пороги
   детекторов не трогали. Оформить как именованный test-hook.
4. **TC11 Vgs=7.76263 В** ниже требуемого R1 окна **7.8…8.4 В**; проходит лишь по ослабленному критерию
   `vgs_x>=7`. Либо обеспечить 7.8 В, либо зафиксировать отклонение.
5. **Смена `RGS` 1 МОм → 10 МОм** сделана после провала TC11 и поднимает Vgs до 8.36 В, но ослабляет
   стягивание затвора к истоку (помехоустойчивость). Обосновать/проверить для реальной платы.
6. **Модель MOSFET ≠ кристалл по температуре.** Подтверждено `.meas`: 2.70200e-3 → 3.08895e-3 Ом
   (×1.143), а не ×1.3…1.7. Порог 10 мОм всё равно выполняется (по даташиту ≤3.23 мОм/прибор), но
   `TC2b` нельзя выдавать за валидацию 100 °C.
7. **R12/SOA не автоматизирован.** `TC9.e_off = 4.11931e-3` Дж измерен, но критерия на E/SOA нет
   (`criteria.tsv:59-60` — только `ikey0` и `t_off`); точка SPEC 50 В/20 А, а TC9 — 45 В/20.7 А. Добавить
   явный критерий сверки с SOA.
8. **Партномер.** `SPEC.md:21` — `IAUC120N06S5L016`; мастер/`cards.model`/`BOM.csv` — `IAUC120N06S5N015`
   (разные приборы, класс 1.6 vs 1.5 мОм). `MOSFET_CHOICE.md §2` это отмечает, но BOM должен нести один
   order code.

### P3 — мелкие

1. `TC1`-критерий `vpin6_off<=45 В` не нагружен (11.27 В); худший случай R5 (Vaux>30 В) не тестируется.
2. `TC12` зря ушёл с 2 В на 3 В: 2 В на финале проходит (`vgs_max=0.0146482`).
3. Нет критерия на `T_DC≤1 с` (R7) — `t_det` измеряется, но не ограничивается.
4. `TC4.t_off` (75.5 мкс) оптимистичнее `TC3` (80.4 мкс): при −3 В затвор частично стягивается
   Cgd-связью **до** `t_det` (проба: `t_g5=5.00078` < `t_det=5.03288`), поэтому худший случай — `TC3`.
5. `grep -lF 'uPC1237_SSR.cir'` в `run_tests.sh` срабатывает на комментарий шапки, а не на реальную
   зависимость.
6. Знак/единицы: `rds_key` в `TC2` отрицательный; критерий — с `abs`; отчёт должен явно писать `|Rds|`.
7. `ci_logs/` смешивал старые и новые логи (в частности «зависший» `TC5_overload.log` от старого мастера
   с `Reference value`), что и породило P1-4.

---

## 3. Итог: **НЕ ПРИНИМАТЬ в текущем виде**

Функционально финальный снимок состоятелен: CI **12/12 PASS** (воспроизведено независимо), подтверждены
Rds(on) ≤10 мОм/прибор, `t_off` ≤2 мс с активным разрядом, fail-safe по всем заявленным авариям и
невозможность открытия ключа при подаче питания с аварией. Из пяти утверждений **четыре подтверждены**,
одно — **опровергнуто**.

Однако комплект артефактов рассогласован, поэтому принять проект нельзя:
- `.asc` не соответствует `.cir` (опровергнутое утверждение 5);
- `BOM.csv` не выполняет R10 (9× `TBD`, противоречия с мастером);
- `REPORT.md`/`README.md` устарели и противоречат машинным результатам;
- обход `BOVL «/50»` и новое определение `t_off` не отражены в критериях/отчёте;
- `tests/results.json` формировался на изменяющемся дереве.

**Условия для «принять»:** перегенерировать `uPC1237_SSR.asc` и `BOM.csv` из финального мастера; обновить
`REPORT.md`/`README.md` под финальные числа и активный разряд; зафиксировать в `criteria.tsv`/отчёте
определение `t_off` (до `Vto`) и тест-хук `BOVL /50`; записать хэши входов в `results.json`; после этого
повторить CI на замороженной ревизии.
