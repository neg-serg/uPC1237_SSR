# Быстрый старт: чем открыть и что нажать

Файлы проекта: **`uPC1237_SSR.asc`** (схема), **`uPC1237_SSR.cir`** (тот же нетлист текстом),
**`tests/`** (22 теста), `BOM.csv`, `README.md` (подробности), `REPORT.md` (результаты).

## Вариант A. LTspice — посмотреть схему и осциллограммы (рекомендуется)

**Что установить:** LTspice от Analog Devices, бесплатно, Windows/macOS (есть и под Linux через Wine).
Страница загрузки: <https://www.analog.com/en/resources/design-tools-and-calculators/ltspice-simulator.html>
(текущая версия — LTspice 24; подойдёт и старая LTspice XVII, и LTspice IV — файл в формате «Version 4»).

**Что делать:**
1. Распаковать папку проекта куда угодно (например, `C:\uPC1237_SSR`).
2. Запустить LTspice → `File → Open` → выбрать **`uPC1237_SSR.asc`**.
3. Нажать **Run** (кнопка «человечек с графиком» или `Ctrl+R`).
4. Появится окно графика. Курсором навести на провод/узел — LTspice подпишет имя цепи; щёлкнуть —
   добавится осциллограмма. Смотреть в первую очередь:
   `V(GATE1)`, `V(CS)`, `V(PIN6)`, `I(VLEDS)`, `V(SPK)`, `V(AMP_OUT)`.
5. Время анализа задано в схеме текстом: `.tran 20u 8 0 20u uic` (8 секунд модельного времени,
   шаг 20 мкс). Можно править прямо в схеме и снова нажимать Run.

**Если .asc открылся с «висячими» проводами:** схема сгенерирована скриптом и в самой LTspice
ни разу не открывалась (в этой среде LTspice нет). Тогда используйте надёжный путь:
`File → Open` → **`uPC1237_SSR.cir`** (LTspice откроет его как текстовый нетлист) → `Run`.
Расчёт тот же, а связи заданы именами цепей, а не геометрией. Правки в этом файле делайте текстом.

**Что должно быть видно (контрольные точки):**
| Что | Ожидание |
|---|---|
| `V(PIN6)` в первые 4.4 с | ≈ 12 В (ключ закрыт), ток светодиода 0 |
| после 4.4 с | `V(PIN6)` ≈ 0.3 В, `I(VLEDS)` ≈ 11 мА, `V(GATE1)` − `V(CS)` ≈ 8.36 В |
| `V(SPK)` | 0 В (сигнала нет), затем повторяет форму `V(AMP_OUT)` |
| в конце (t≈8 с) | всё стабильно, ничего не «звенит» |

## Вариант B. ngspice — прогнать 22 теста (числа и PASS/FAIL)

**Что установить:** ngspice. Linux — из репозитория (`apt install ngspice`, `nix shell nixpkgs#ngspice`);
Windows — сборка с <https://ngspice.sourceforge.io/download.html> (или SourceForge
<https://sourceforge.net/projects/ngspice/files/>); macOS — Homebrew.

**Один тест:**
```
ngspice -b tests/TC1_power_on.cir
```
В конце файла увидите строки вида `vg1 = 8.36e+00`, `iled = 1.1e-02` — это измерения.

**Все тесты сразу (Linux):**
```
./preflight.sh     # 5 секунд: проверка, что .asc/тесты/BOM соответствуют мастеру
./run_tests.sh     # ~10 минут: 22 теста, таблица PASS/FAIL, результат в tests/results.json
```
`run_tests.sh` рассчитан на Linux с nix (он сам подтягивает ngspice). На Windows/WSL проще гонять
тесты по одному командой выше и сверять с таблицей в `REPORT_TESTS.md`.

## Вариант C. Ничего не устанавливать
Открыть `REPORT.md` и `tests/results.json` — там все измеренные числа и вердикты (22/22 PASS),
а в `REPORT_TESTS.md` — протокол по каждому тесту.

## Частые вопросы
- **«LTspice ругается на `.meas ... WHEN`»** — это синтаксис ngspice, он встречается только в
  `tests/*.cir`. В схеме и в `uPC1237_SSR.cir` блок анализа LTspice-совместимый.
- **Кракозябры в комментариях** — файлы в UTF-8, LTspice IV/VII показывают кириллицу как мусор;
  на расчёт не влияет, все директивы и имена цепей — ASCII.
- **Хочу поменять номиналы** — правьте `uPC1237_SSR.cir` (единый источник истины) и затем
  `python3 gen_tests.py && python3 gen_schematic.py && python3 gen_bom.py`, иначе `preflight.sh`
  забракует рассинхронизацию. Формулы пересчёта — README §0.5.
- **Хочу увидеть аварию** — в тестах это уже сделано: `TC3_dc_pos.cir` (постоянка +3 В),
  `TC13a_ind_load.cir` (индуктивная нагрузка), `TC18_blocking.cir` (проверка блокировки).
  Скопируйте сценарий оттуда в схему или просто прогоните тест в ngspice.
